package com.trabalho.a3.repository;

import com.trabalho.a3.model.Cliente;
import java.util.UUID;
import org.springframework.data.jpa.repository.JpaRepository; // Varios métodos prontos pars transação com o banco de dados
                                                             // ex: Buscar uma listagem de determinado recurso, salvar, deletar, atuslizar e buscar um serviço único
public interface Repository extends JpaRepository<Cliente, UUID>{
	
}
